type Currency = {
    _id: string,
    country: string,
    ISO: string,
    currency: string,
    currencySymbol: string,
    exchangeRates: number,
    active: boolean,
    default: boolean
}

export default Currency