type UserProfileDataModel = {
    id: string
    name: string
    email: string
    number: string
    image: string
    cartCount: number
    roles: string[]
}

export default UserProfileDataModel